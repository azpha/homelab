# Syncthing

[Syncthing](https://syncthing.net/) is an open-source file synchronization tool, used to synchronize files in real time between machines.

## Installation

```
sudo docker compose up -d
```

See [docker-compose.yml](./docker-compose.yml).
